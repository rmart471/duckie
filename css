/* Reset default browser styles */
body, h1, p {
  margin: 0;
  padding: 0;
}

body {
  background-color: black;
  margin: 0;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  overflow: hidden;
  position: relative;
}

.arc-reactor {
  width: 100px;
  height: 100px;
  background: radial-gradient(
    #062138 10% 17%, 
    transparent 40% 69%, 
    #062138 10% 15%, 
    transparent 
  );
  border-radius: 50%;
  animation: pulse 6s ease-in-out infinite;
  position: relative;
  z-index: 1;
}

#input-container {
  position: absolute;
  background-color: rgba(5, 33, 56, 0.68);
  border: 2px solid transparent;
  border-image-slice: 1;
  border-radius: 10px;
  padding: 10px;
  display: none;
  width: 200px;
  top: 15%;
  left: 50%;
  transform: translate(-50%, -50%);
}

#input-container input {
  background-color: transparent;
  color: white;
  border: none;
  outline: none;
  width: 100%;
}

@keyframes pulse {
  0%, 100% {
    transform: scale(2.3);
  }
  50% {
    transform: scale(2.4);
  }
}
